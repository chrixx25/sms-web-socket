const express=require("express"),http=require("http"),webSoscket=require("ws"),port=5e3,server=http.createServer(express),wss=new webSoscket.Server({server:server});wss.on("connection",(function connection(ws){ws.on("message",(function incoming(data){wss.clients.forEach((function each(client){client!=ws&&client.readyState==webSoscket.OPEN&&client.send(data)}))}))})),server.listen(5e3,(function(){console.log("server is uup")}));